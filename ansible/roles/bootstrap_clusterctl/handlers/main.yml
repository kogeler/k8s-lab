---
# Handlers contract (plan §2.6.4): reactive only. bootstrap_clusterctl
# drives `clusterctl init` against the bootstrap k3s API and applies
# Provider CRs via kubernetes.core.k8s_info — there is no host-level
# daemon to reload, no in-container systemd unit to bounce, and CAPI
# Provider config changes flow through `clusterctl upgrade` (out of
# Stage 1 scope). Placeholder so future reactive behaviour lands in
# the canonical location rather than being invented in tasks/.
