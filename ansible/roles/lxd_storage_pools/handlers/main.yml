---
# Handlers contract (plan §2.6.4): reactive only. Storage pools are
# long-lived logical objects — there is no daemon/service to reload
# after config changes, LXD picks them up live. Placeholder so future
# reactive behaviour lands in the canonical location rather than being
# invented in tasks/.
