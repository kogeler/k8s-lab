---
# Handlers contract (plan §2.6.4): reactive only. A cleanup role has no
# daemon-level side effects — the LXD container delete is synchronous.
# Placeholder so future reactive behaviour lands in the canonical
# location rather than being invented ad-hoc in tasks/.
