---
# Handlers contract (plan §2.6.4): reactive only. LXD applies profile
# changes live to running instances (except security.* changes that
# require a restart). No daemon-level reload is needed. Placeholder so
# future reactive behaviour lands in the canonical location rather
# than being invented in tasks/.
