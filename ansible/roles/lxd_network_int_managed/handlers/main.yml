---
# Handlers contract (plan §2.6.4): reactive only. LXD managed networks
# pick up config changes live via the PATCH endpoint — no daemon to
# reload. Placeholder so future reactive behaviour lands in the
# canonical location rather than being invented in tasks/.
